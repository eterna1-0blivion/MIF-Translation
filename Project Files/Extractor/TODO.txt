InputFolder:

Открываем каждый архив по очереди {
	Если (видим в архиве папку assets) {
		то проверяем всё внутри неё
		Если (находим папку lang) {
			то копируем всё внутри папки lang вместе с файловой структурой в OutputFolder
		}
		Если (не видим lang) {
			то закрываем файл, идём к следующему
		}
	}
	Если (не видим assets) {
		то закрываем файл, идём к следующему
	}
}
